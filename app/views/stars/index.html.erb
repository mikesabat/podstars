<p id="notice"><%= notice %></p>

<h1>Stars</h1>

<%= form_with url: new_star_path, method: :get, local: true do |form| %>
  <% if @star.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(star.errors.count, "error") %> prohibited this star from being saved:</h2>

      <ul>
      <% star.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Search for Star" %>
    <%= form.text_field :name, id: :star_name %>
  </div>

    <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</br>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Bio</th>
      <th>Link</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stars.each do |star| %>
      <tr>
        <td><%= star.name %></td>
        <td><%= star.bio %></td>
        <td><%= star.link %></td>
        <td><%= link_to 'Show', star %></td>
        <td><%= link_to 'Edit', edit_star_path(star) %></td>
        <td><%= link_to 'Destroy', star, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Star', new_star_path %>
