<%= form_with(model: star, local: true) do |form| %>
  <% if star.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(star.errors.count, "error") %> prohibited this star from being saved:</h2>

      <ul>
      <% star.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :star_name %>
  </div>

  <div class="field">
    <%= form.label :bio %>
    <%= form.text_area :bio, id: :star_bio %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :star_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
